apiVersion: v1
kind: Pod
metadata:
  name: nginx-pod
  labels:
    app: nginx-Load-Balancer-pod

  spec:
    containers:
    - name: nginx-pod-container
      image: nginx:latest
      ports:
        - containerPort: 80