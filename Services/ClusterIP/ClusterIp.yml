apiVersion: v1
kind: Service
metadata:
  name: nginx-deployment-clusterip

spec:
  selector:
    type: ClusterIP
    app: nginx-deployment
  ports:
    - protocol: TCP
      port: 80
      targetPort: 80